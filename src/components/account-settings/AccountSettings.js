import React from "react";
import NavBar from "../navbar/NavBar";
import AccountBody from "./account-body/AccountBody";

const AccountSettings = () => {
  return (
    <div>
      <NavBar />
      <AccountBody />
    </div>
  );
};

export default AccountSettings;
